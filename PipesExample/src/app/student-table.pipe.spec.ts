import { StudentTablePipe } from './student-table.pipe';

describe('StudentTablePipe', () => {
  it('create an instance', () => {
    const pipe = new StudentTablePipe();
    expect(pipe).toBeTruthy();
  });
});
